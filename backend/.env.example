PORT=4000

# Postgres connection (matches docker-compose.yml defaults)
DATABASE_URL=postgres://connectsphere:connectsphere@localhost:5432/connectsphere

# JWT
JWT_SECRET=change_me_in_real_deployment
JWT_EXPIRES_IN=1d

# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:5173
